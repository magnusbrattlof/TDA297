WHAT WE DID

- TODO: 
    -   Verify that stress test still works
    -   Verify that stress test works even with crashing nodes

- WHAT WE DID:
    - Fixed crashing nodes, works with sending only one message
    - Works even with only one node left in the network
    - 

2019-05-17
	- Prints out the whole backlog when there is only one node left.
